#pragma once

#include <stdio.h>
#include <Windows.h>

#define DEBUG 0 // 1이면 Debug 내용 보임
#define SHOWCOUNT 1 // 1이면 최종적으로 알고리즘 동작 횟수 표기

#define MAX 9 // 행, 열, 박스, 숫자의 최대 값
#define BOX 3 // 박스 가로 세로의 크기